@model TimesheetPrj.Models.Login
@{
    ViewBag.Title = "Display";
}



<h2 style="margin-top:20px;">TimeEntrySystem</h2>


<div>

    @*<div class="row">
        <div class="col-md-6">
            @using (Html.BeginForm())
            {
                <div class="form-group">
                    @Html.LabelFor(x => x.Username)
                    @Html.TextBoxFor(x => x.Username, new { @class = "form-control" })
                </div>
                <div class="form-group">
                    @Html.LabelFor(x => x.Password)
                    @Html.TextBoxFor(x => x.Password, new { @class = "form-control" })
                </div>
                <div class="form-group">
                    @Html.LabelFor(x => x.ConfirmPassword)
                    @Html.TextBoxFor(x => x.ConfirmPassword, new { @class = "form-control" })
                </div>
                <div class="form-group">
                    <button id="CmdSubmit" type="submit" class="btn btn-outline-secondary">Create</button>
                </div>
            }

        </div>
        
    </div>*@

    @*<div>
        <div>
            <label>Select A Name</label>
        </div>
        <div>
            <select class="form-control" id="Names" name="Name Of Student">
                <option value="0">Select Name</option>
                <option>Sai Teja</option>
                <option>Lakshman</option>
                <option>Teja</option>
                <option>Sairam</option>
            </select>
        </div>
    </div>*@
    <div style="padding-top:30px">
        @Html.ActionLink("GO", "Create", "Times", null, new { @class = "btn btn-primary" })
    </div>
</div>